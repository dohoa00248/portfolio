<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Update Profile</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Reset CSS -->
    <link rel="stylesheet" href="/css/reset.css" />

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="/css/bootstrap.min.css" />

    <!-- Plugin CSS (AOS) -->
    <link rel="stylesheet" href="/css/aos.css" />

    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/profile.css" />
  </head>

  <body>
    <div class="container py-5">
      <h2 class="profile-title mb-4 text-center">👤 Update Profile</h2>

      <form
        action="/api/v1/user/profile?_method=PUT"
        method="post"
        class="profile-form mx-auto"
      >
        <div class="mb-3">
          <label for="username" class="profile-label">Username</label>
          <input
            type="text"
            class="profile-input"
            id="username"
            name="username"
            value="<%= currentUser.username %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="email" class="profile-label">Email</label>
          <input
            type="email"
            class="profile-input"
            id="email"
            name="email"
            value="<%= currentUser.email %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="firstName" class="profile-label">First Name</label>
          <input
            type="text"
            class="profile-input"
            id="firstName"
            name="firstName"
            value="<%= currentUser.firstName || '' %>"
          />
        </div>

        <div class="mb-3">
          <label for="lastName" class="profile-label">Last Name</label>
          <input
            type="text"
            class="profile-input"
            id="lastName"
            name="lastName"
            value="<%= currentUser.lastName || '' %>"
          />
        </div>

        <div class="d-flex justify-content-between">
          <a href="/api/v1/user/dashboard" class="btn profile-btn-secondary"
            >⬅️ Cancel</a
          >
          <button type="submit" class="btn profile-btn-primary">
            💾 Update Profile
          </button>
          <button type="button" onclick="history.back()">Back</button>
        </div>
      </form>
    </div>

    <script src="/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
