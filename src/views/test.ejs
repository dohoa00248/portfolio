<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    hello test
    <br />
    <!-- <ul>
      <% dataList.forEach(project => { %>
      <li><%= project.title %> - <%= project.description %></li>
      <% }); %>
    </ul> -->
    <ul>
      <% dataList.forEach(project => { %>
      <li>
        <%= project.title %> - <%= project.description %> - <%= project.live %>
        - <%= project.github %>
      </li>
      <%})%>
    </ul>
  </body>
</html>
